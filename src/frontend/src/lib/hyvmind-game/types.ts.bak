export interface TextPools {
  curations: string[];
  swarms: string[];
  lawEntities: string[];
}

export interface GameCallbacks {
  onGameOver: (score: number) => void;
}

export type GameInstance = { cleanup: () => void };
