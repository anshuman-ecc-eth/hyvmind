{
  "frontend/src/App.tsx": "Main application component with authentication flow, view switching, profile setup modal, and a silent one-time-per-login cleanup effect that fetches archived node IDs and persists hidden collectible IDs to localStorage without any user-facing feedback.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; adds useGetArchivedNodeIds hook that fetches archived node IDs from the backend, enabled only when actor is available and user is authenticated.",
  "frontend/src/hooks/useEditor.ts": "Placeholder editor initialization function for future rich text editing capabilities.",
  "frontend/src/components/Header.tsx": "Application header with navigation tabs for authenticated users (now including a 'Collectibles' tab and 'Leaderboard' replacing 'BUZZ'), theme toggle, create node button, and hamburger menu where the authenticated settings item is now labeled 'Settings' instead of 'Profile Settings'.",
  "frontend/src/components/Footer.tsx": "Footer component with copyright notice and caffeine.ai attribution link styled with monochrome text and accent color only on hover.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time user profile setup with updated instructional text \"Please pick a username to get started. You can change it later via Profile Settings.\" and monochrome styling with accent color only on submit button hover state.",
  "frontend/src/components/CreateNodeDialog.tsx": "Dialog component for creating all five node types; adds `extractLawTokenSequence` helper and passes the required `originalTokenSequence` field when calling `useCreateLocation`, fixing the TypeScript argument mismatch.",
  "frontend/src/pages/GraphView.tsx": "Interactive D3.js canvas-based graph visualization component with hierarchical culling, spatial indexing, subgraph navigation, node filtering, and visualization controls; Interpretation Tokens are now rendered as independent nodes with dashed lines connecting them to their \"From\" and \"To\" nodes instead of being hierarchically nested beneath Law Tokens.",
  "frontend/src/pages/TreeView.tsx": "Hierarchical tree view component displaying all five node types with a dedicated Interpretation Tokens section (renamed from Schema Builder), featuring inline creation buttons, law token sequences, export functionality, and separate Filter & Sort modals for both the hierarchical view and the Interpretation Tokens section; updated to use h-full instead of viewport calculations and proper flex/min-h-0 constraints so both panels scroll independently within their allocated space without bleeding into the footer.",
  "frontend/src/pages/DebugPanel.tsx": "Debug interface displaying all five node types with comprehensive relationship visualization including shared law token connections, enhanced interpretation token display showing dual relationships (from/to) with connection counts (in/out/total), custom attributes, and admin-only data reset functionality; updated to use fromTokenId instead of fromLawTokenId.",
  "frontend/package.json": "Package configuration with all required dependencies including newly added framer-motion ^11.15.0 for microinteractions.",
  "frontend/src/index.css": "Global styles with OKLCH monochrome color system for the application UI, plus theme-independent CSS rules scoped to Voronoi diagram classes (.voronoi-diagram-container, .voronoi-canvas, .voronoi-tooltip-content) that enforce fixed white background and tooltip appearance across both light and dark themes without affecting header/footer styling.",
  "frontend/src/components/SwarmMembershipButton.tsx": "Compact button component displaying swarm membership status with monochrome badge styling and accent color only on hover for join button.",
  "frontend/src/components/SwarmMembershipManager.tsx": "Swarm management interface displaying both created and joined swarms with contributor approval functionality, implementing the Contributor model with merged swarm results and updated section title \"Your Swarms\" with subtitle \"Co‑govern your research community.\"",
  "frontend/src/components/SwarmMembershipApproval.tsx": "Contributor approval interface displaying pending and approved contributor requests with enhanced profile name display, robust Principal handling using proper type casting, and correct MembershipStatus enum comparison instead of discriminated union pattern.",
  "frontend/src/components/VoronoiDiagram.tsx": "Canvas-based Voronoi diagram component with colorful node-type-specific cell fills using custom pixel-based polygon computation, high-DPI canvas scaling for sharp edges, theme-independent fixed styling (white background), minimal hover tooltips showing node names and types, localStorage-based caching with cache-first loading strategy, manual refresh functionality with icon-only button fixed in the bottom-right corner, and simple solid black seed points.",
  "frontend/src/components/DataResetDialog.tsx": "Alert dialog component for admin users to reset all application data with confirmation input, loading states, and automatic page reload after successful reset.",
  "frontend/index.html": "HTML entry point with page title set to \"version 0.0.3\".",
  "frontend/src/components/VotingButtons.tsx": "Voting component that calls all React hooks at the top level before conditional logic, returns null for Curation nodes (blocking all voting functionality), with globally shared vote counts fetched from backend, one-time irreversible voting behavior, proper loading states, and automatic UI updates after successful votes for all other node types.",
  "frontend/src/pages/OntologiesView.tsx": "Comprehensive interface displaying both Core Ontology and Swarm-Specific Extended Ontologies with collapsible sections, icon-only download buttons, and Swarm curation context display; refactored to use shared CORE_ONTOLOGY_PREFIXES with hm: prefix from coreOntology utility.",
  "frontend/src/pages/BuzzLeaderboard.tsx": "BUZZ leaderboard page displaying ranked users with scores divided by 10,000,000 (10⁷ scale) for human-readable display, preserving all existing rank icons, table layout, and \"How points work\" section.",
  "frontend/src/components/ProfileSettingsModal.tsx": "Profile settings modal with Profile Information, Wallet (BUZZ balance), and Mint Settings (number of copies + price preview) sections; the NFTGallery / Manage Collectibles section has been removed to improve modal performance.",
  "frontend/src/components/SearchModal.tsx": "Modal dialog component for searching nodes by custom attributes with auto-complete dropdowns for both keys and values, ensuring users can only pick from existing keys and values rather than entering free text, with scrollable search results display and clickable node items showing name, type, and parent context.",
  "frontend/src/components/SwarmUpdateCard.tsx": "Enhanced swarm update card component that displays full token details including originalTokenSequence for Law Tokens and context for Interpretation Tokens, with upvote, downvote, and new ignore buttons that irreversibly mark tokens as acted upon, disappearing after any action.",
  "frontend/src/components/SwarmUpdatesSection.tsx": "Section component that fetches and displays unvoted tokens for a swarm using the new getUnvotedTokensForSwarm API, showing all tokens (Law and Interpretation) that the current user hasn't voted on or ignored, with loading and empty states.",
  "frontend/src/components/FilterSortModal.tsx": "Modal component providing filter and sort functionality for the List tab with dropdowns for Type, Attributes, and multi-select checkboxes for Tags filters, generating a simple flat list of matching nodes without hierarchy or collapsibility when filters are applied, with proper tag filtering logic that requires nodes to have ALL selected tags.",
  "frontend/src/components/SchemaBuilderFilterModal.tsx": "Modal component providing filter and sort functionality specifically for the Schema Builder section, allowing sorting by name, creation date, or context length, and filtering by tags or node connections (From/To), with full interactivity for opening token detail modals.",
  "frontend/src/pages/TerminalPage.tsx": "Terminal interface with persistent session, scrollable output, and async command handling for `/help`, `/find`, `/clear`, `/ont`, `/filter`, and `/archive`; exports `TerminalMessage` interface for use by `terminalSession.ts`; `handleArchive` is now async and calls `executeArchiveCommand` with the backend `actor` and `queryClient`, awaiting the result and displaying success/error messages; disambiguation flow for `/archive` also awaits the async backend call via `executeArchiveForNode`.",
  "frontend/src/utils/terminalParser.ts": "Command parser utility that tokenizes forward-slash commands and extracts key=value pairs, supporting quoted string values with escaped quotes, repeatable keys for attributes, special handling for /find command (simple argument instead of key=value), enhanced parsing for tokens={a}{b}{c} syntax that preserves spaces inside brace blocks, and explicit rejection of backslash-prefixed commands with a helpful error message.",
  "frontend/src/utils/terminalCommands.ts": "Command execution layer for the terminal with all create command handlers (curation, swarm, location, interpretation token); `executeArchiveCommand` is now async, accepts `actor: backendInterface` and `queryClient: QueryClient` parameters, calls `actor.archiveNode(nodeId)` as the primary archive action, retains `archiveNodeById` for localStorage UI-badge state as a secondary step, and invalidates the `graphData` React Query key on success.",
  "frontend/src/utils/terminalMessages.ts": "Message formatting utilities for terminal output including help text (now listing `/archive` as an available command with usage description), error formatters, find/filter result formatters, and a new `formatArchiveMissingNameError` function.",
  "frontend/src/utils/terminalNameResolution.ts": "Name-to-ID resolution utility for Terminal commands with case-insensitive matching, support for all five node types, command+field-specific type filtering, normalized command identifier handling (strips leading forward slash), special handling for /ont and /filter commands (name-only resolution without ID passthrough), support for parent/child filtering in /filter command, and now includes a case for the /archive command that enables lookup by both short name and full generated ID across all five node collections.",
  "frontend/src/components/TerminalDisambiguationPicker.tsx": "Arrow-key navigable picker component for Terminal disambiguation with Up/Down selection, Enter confirm, Escape cancel, click-to-select support, enhanced display of parentContext when available to distinguish duplicate names, styled to match Terminal aesthetics, and onConfirm callback that passes the selected ResolvedNode to the handler.",
  "frontend/src/utils/terminalSession.ts": "Terminal session persistence utility using localStorage with versioning (bumped to v4) to invalidate old sessions; updated `StoredSession` interface to use `turtleText`/`mermaidText` property names in `ontologyData` to match the current `TerminalMessage` type definition.",
  "frontend/src/utils/coreOntology.ts": "Shared core ontology definitions with hm:Relation super-property, hasParent/hasChild inverse relationships, FromRelation/ToRelation InterpretationToken-specific properties, and Node data properties (label, createdBy, createdAt, hasTag).",
  "frontend/src/utils/terminalOntologyTurtle.ts": "TTL generation utility for \\ont command that emits refined section headers, uses only hm:label, emits rdf:type for the selected node and all referenced parent/child nodes based on their actual types in graphData, generates typed XSD literals for tags, and now correctly places hm:hasChild node-reference triples as standalone triples in the Relations section instead of within the Properties subject block.",
  "frontend/src/utils/ontologySanitize.ts": "Shared sanitization utility for converting human-readable names to valid Turtle local names and Mermaid node IDs, with label truncation for compact diagram display.",
  "frontend/src/utils/terminalOntologyMermaid.ts": "Mermaid flowchart generator for \\\\ont command using refined ontology vocabulary (hasChild/hasParent for hierarchy, FromRelation/ToRelation for InterpretationToken directional relationships) with compact horizontal diagrams and solid lines only for all connectors (no dashed lines).",
  "frontend/src/components/TerminalOntologyOutput.tsx": "Terminal output component that displays Turtle and Mermaid sections side-by-side with copy buttons, rendering Mermaid as an interactive diagram while preserving copy-to-clipboard behavior for raw Mermaid code, with combined server-side and client-side error handling via onError callback.",
  "frontend/src/components/MermaidDiagram.tsx": "React component that dynamically loads Mermaid from CDN and renders Mermaid diagram syntax into interactive SVG diagrams using the neutral theme, with proper state reset on mermaidText changes, render token cancellation to prevent stale renders, explicit Mermaid syntax validation, and comprehensive error handling with loading states.",
  "frontend/src/utils/ttlToMermaid.ts": "Utility that parses TTL text to extract relationships and node labels from hm:label, generating Mermaid flowchart diagrams with explicit edge labels (hasChild, hasParent, FromRelation, ToRelation) for all relationships using solid lines only (no dashed connectors).",
  "frontend/src/utils/terminalOutputFormatting.ts": "Terminal output formatting utilities that parse raw message text into structured line tokens with auto-indentation and visual hierarchy, with emoji markers removed from command/example tokens and only success/error emojis supported at message level, and command line detection updated to recognize forward-slash-prefixed commands.",
  "frontend/src/utils/voronoiPalette.ts": "Fixed color palette mapping for the 5 Hyvmind node types with colors matched to reference image (Curation=red, Swarm=blue, Location=green, Law Token=purple, Interpretation Token=orange) with appropriately darker stroke colors, all seed colors set to black.",
  "frontend/src/components/LandingGraphDiagram.tsx": "Canvas-based force-directed graph visualization with smooth zoom-in hover effects (scale 1.0 to 1.15) implemented using Canvas rendering transforms instead of external animation libraries, maintaining existing blur effects for non-adjacent nodes, theme-aware styling, and keyboard accessibility support.",
  "frontend/src/components/NFTGallery.tsx": "Gallery component displaying user's Law Token and Interpretation Token collectibles, now filtering out any collectibles whose IDs appear in the localStorage-based hidden collectibles set (populated by the archived-node cleanup in App.tsx).",
  "frontend/src/components/NFTDetailModal.tsx": "Modal displaying collectible details with mint button, edition status, and BUZZ balance check; fixes the `useGetCollectibleEditions` call to pass `token?.id ?? ''` (empty string instead of null) so the type matches `string | null` accepted by the hook.",
  "frontend/src/pages/CollectiblesView.tsx": "New standalone page component that renders the NFTGallery in a full-page layout with a title and description, used as the dedicated Collectibles tab.",
  "frontend/src/utils/archivedCollectiblesStore.ts": "New utility module managing a localStorage-based store for hidden collectible IDs, keyed by principal; exports getHiddenCollectibleIds, setHiddenCollectibleIds, and clearHiddenCollectibleIds."
}